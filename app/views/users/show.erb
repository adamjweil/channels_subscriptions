<h1>Profile page for <%= @user.first_name  %> <%= @user.last_name  %></h1>
<h2>Email Address: <%= @user.email %></h2>
<% @user.channels.each do |channel| %>
  <li><%= channel.name  %></li>
<% end %>

<h3>Total cost</h3>
$ <%= @user.channels.reduce(0.0) {|sum,channel| sum + channel.price_per_month } %>
