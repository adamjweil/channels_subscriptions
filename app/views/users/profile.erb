<h1> Welcome <%= current_user.first_name %> </h1>
<p> Your profile </p>
<p> Current Subscriptions:</p>
<ul>

  <% current_user.subscriptions.each do |subscription| %>
    <li><a href='/channels/<%=subscription.channel_id %>'><%= subscription.channel.name %></a></li>

  <% end %>
</ul>
<p> Current Price Per Month: $<%= current_user.channels.sum(:price_per_month) %> </p>

<div>
  <a href='/channels'>Show Current Available Channels</a>
</div>
<br>
<div>
  <a href='/logout'>Logout</a>
</div>
